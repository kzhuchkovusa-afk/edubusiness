# Netlify configuration for the GoCoding static site.
# The project uses Next.js static export (output: 'export'), so Netlify just
# serves the pre-built `out/` directory — no Next.js runtime needed.

[build]
  command = "npm run build"
  publish = "out"

[build.environment]
  NODE_VERSION = "22"

# Set NEXT_PUBLIC_SITE_URL to your production domain in:
#   Netlify dashboard → Site settings → Environment variables
# Example: https://www.yourdomain.com
#
# No catch-all redirect: the static export emits `404.html`, which Netlify
# serves automatically for unmatched routes. A catch-all `/*` redirect would
# also intercept Netlify Forms POST submissions and break the lead forms.
