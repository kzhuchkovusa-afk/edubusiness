# GoCoding Marketing Site

A modern marketing website for **GoCoding** — a company that licenses ready-made STEM curriculum (coding & robotics) to independent afterschool centers and tutoring businesses in the US.

> **Status:** MVP Phase 1. Priority landing page (`/for-tutoring-centers`) plus programs, pricing, about, contact, homepage.

## Tech stack

| | |
|---|---|
| Framework | Next.js 14 (App Router) |
| Language | TypeScript |
| Styling | Tailwind CSS 3.4 |
| Icons | lucide-react |
| Animations | CSS transitions + IntersectionObserver (reveal-on-scroll) |
| Deployment | Static export (`output: 'export'`) — works with Vercel, Netlify, GitHub Pages |
| Content | JSON files in `src/content/` (no CMS) |

## Local development

```bash
npm install
npm run dev          # http://localhost:3000
npm run build        # static export to /out
```

## Project structure

```
src/
  app/                         App Router pages
    page.tsx                   Homepage
    for-tutoring-centers/      Landing #1 (13 blocks)
    programs/[slug]/           Program template + Scratch/Python/Robotics
    pricing/                   Pricing + FAQ
    about/                     About + stats
    contact/                   Calendly slot + form
    sitemap.ts, robots.ts      SEO
  components/
    layout/                    Header, Footer, Navigation, MobileMenu, StickyCTA, Logo
    landing/                   13 reusable landing blocks
    programs/                  ProgramCard, LevelRoadmap
    shared/                    Button, Card, Badge, Section, Container, etc.
    contact/                   Contact form
  content/
    landing-pages/             Per-audience JSON copy
    programs/                  Per-program JSON
    shared/                    Company info, pricing, etc.
  lib/                         Content helpers, utils
  styles/                      Tailwind globals
public/
  favicon.svg
  images/og-image.svg
```

## Adding a new audience page

Audience-specific landing pages (`/for-language-schools`, `/for-math-centers`, etc.) all share the same 13-block structure. To add one:

1. Create `src/content/landing-pages/<audience>.json` (use `tutoring-centers.json` as template).
2. Add the slug to `landingPages` in `src/lib/content.ts`.
3. Create `src/app/for-<audience>/page.tsx` (copy from `for-tutoring-centers/page.tsx`, swap the content key).

No new components needed — just new copy.

## Adding a new program

1. Create `src/content/programs/<slug>.json` (use `scratch.json` as template).
2. Register it in `src/lib/content.ts` (`programs` map).
3. Add an entry to `src/content/programs/index.json` for the `/programs` overview grid.

`/programs/<slug>` is generated automatically via `generateStaticParams`.

## SEO

- Per-page `<title>`, `<meta description>`, Open Graph tags.
- `sitemap.xml` and `robots.txt` are auto-generated by Next.js.
- Canonical URLs set in each page's `metadata`.
- Replace `https://gocoding.example.com` in `src/app/layout.tsx`, `sitemap.ts`, and `robots.ts` with the production domain when deploying.

## Deployment

Static export is enabled by default (`output: 'export'`).

```bash
npm run build    # produces /out
```

Connect the repository to Vercel or Netlify and deploy. No server runtime needed.

## To do (later)

- Real testimonials (placeholder cards in `tutoring-centers.json` → `proof.testimonials_placeholder`).
- Calendly inline embed snippet in `/contact` (replace placeholder block).
- Formspree (or similar) action URL in `ContactForm`.
- Production OG image as `.png` (currently `.svg`).
- Google Analytics 4 tag.
- Future audience pages and blog.
