# Production domain — used for canonical URLs, Open Graph tags, sitemap, robots.
# Set this in your hosting provider's environment variables (e.g. Netlify).
# No trailing slash.
NEXT_PUBLIC_SITE_URL=https://gocoding.example.com
